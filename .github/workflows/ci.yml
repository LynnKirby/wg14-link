name: Continuous Integration
on:
  push:
    branches:
    - master
  pull_request:
    branches:
    - master
jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-node@v1
    - run: npm ci
    - run: npm run build -s
    - run: npm run test -s -- --reporters=default --reporters=jest-github-actions-reporter
    - run: npm run lint -s
